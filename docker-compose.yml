version: "2"
services:
    server:
        build: ./server/
        restart: always
        ports:
            - "3000:3000"
        volumes:
            - ./server:/server
            - /server/node_modules
